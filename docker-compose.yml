
version: '3'

services:
  api:
    build: .
    ports:
      - "8000:8000"

  db:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ia_extrema
